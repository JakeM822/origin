# origin
- Clone repository: 
    - git clone https://github.com/JakeM822/origin.git
- Create Virtual Env:
    - source .venv/bin/activate
- Install needed packages
    - pip install -r requirements.txt
# RUN
python app/text_utils.py

# NOTE:
.env must be copied from .env.example
